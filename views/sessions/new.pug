extends /layouts/application.pug

block content
  .container.w-50.mx-auto
    +formFor(f, { url: urlFor('session'), method: 'post' })
      +input(f, 'email')
      +input(f, 'password', { as: 'password' })
      +button(f, type = 'submit', subClass = 'btn-primary', val = 'Sing In')
